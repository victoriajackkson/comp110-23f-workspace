"""checking if input value exists in dictionary."""


def value_exists(dict1: dict[str, int], x: int) -> bool:
    """Checking to see if value exists in dictionary."""
    exists: bool = False
    for elem in dict1:
        if elem [dict1] == x:
            return exists